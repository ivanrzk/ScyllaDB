http: ':5080'
https: ':5443'

database:
  hosts:
    - scylla-manager-db
# Bind prometheus API to the specified TCP address using the HTTP protocol.
# By default it binds to all network interfaces, but you can restrict it
# by specifying it like this 127:0.0.1:56090 or any other combination
# of ip and port.
prometheus: ':56090'
